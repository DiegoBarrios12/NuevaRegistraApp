export interface Employees{ //modelo on el que estaba estudiando pero ya no me dio el tiempo de cambiarlo
    id: string,
    salario: number,
    plantel: string,
    cargo: string,
    img:string
}
